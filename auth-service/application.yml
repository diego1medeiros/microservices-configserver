spring:
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: lb://AUTH-SERVICE
          predicates:
            - Path=/auth/**
  datasource:
   password: 1234
   username: postgres
   url: jdbc:postgresql://localhost:5432/auth
  rabbitmq:
       host: localhost
       port: 5672
       username: diego
       password: 1234
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka  

  instance:
    prefer-ip-address: true
